Developer commands for RandomBot
Begin each line with !dev and follow with command

Command:    Alias:      Description                         Format:             Ex:
pokeparse   pp          Parses the HTML source to JSON      !dev pp